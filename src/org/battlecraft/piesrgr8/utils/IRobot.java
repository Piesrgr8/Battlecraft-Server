package org.battlecraft.piesrgr8.utils;

import org.bukkit.event.Listener;

public class IRobot implements Listener{
	/*
	BattlecraftServer plugin;
	
	public IRobot(BattlecraftServer p) {
		this.plugin = p;
	}
	
	@EventHandler
	public void onChat(AsyncPlayerChatEvent e) {
		Player p = e.getPlayer();
		Player p1 = staff();
		if (!RanksEnum.isStaff(p)) {
			if (e.getMessage().equalsIgnoreCase("help") || e.getMessage().equalsIgnoreCase("helper")) {
				Chat.sendFormattedMessage(p1, message());
			}
		}
	}
	
	public Player staff() {
		Player p = (Player) Bukkit.getOnlinePlayers();
		if (RanksEnum.isStaff(p)) {
			for (int i = 0; i < RanksEnum.arrayRanks.get(p); i++) {
				
			}
		return p;
		}
		return p;
	}
	
	public static String message() {
		String msg = "Yes?";
		return msg;
	} */
}
